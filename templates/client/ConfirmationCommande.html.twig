{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto mt-6 p-6 bg-white shadow-lg rounded-lg text-center">
        <h2 class="text-2xl font-bold text-green-600">Commande Confirmée</h2>
        <p class="text-gray-700 mt-2">{{ message }}</p>
        <a href="{{ path('app_home') }}" class="mt-4 bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Retour à l'Accueil</a>
    </div>
{% endblock %}